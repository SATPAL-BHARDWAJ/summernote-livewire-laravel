$('#summernote').summernote({
    height: 200,
    codemirror: {
        theme: 'monokai'
    },
    callbacks: {
        /* onInit: function() {
            console.log('Summernote is launched', this.value);
        } */
    }
});

